org.gradle.jvmargs=-Xmx4096m

# Only affects the local runClient tasks. The addon uses nothing version specific, so the release
# artifact is identical whatever is listed here, which is why addonInfo declares the version
# wildcard. Add versions here if you want to launch the game on them for testing.
net.labymod.minecraft-versions=1.21.11
